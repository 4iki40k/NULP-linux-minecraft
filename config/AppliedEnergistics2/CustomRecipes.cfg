# Configuration file

general {
    # Dev-only CSV dump of all items; calls Item.getCreativeTab(), which throws
    # NoSuchMethodError on the Cauldron/Crucible server remap. Keep it off. [default: true]
    B:exportItemNames=false
}
